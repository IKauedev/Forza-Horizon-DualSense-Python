[project]
name = "src"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydualsense>=0.7.5",
]

[tool.ruff]
line-length = 120

[tool.uv.sources]
